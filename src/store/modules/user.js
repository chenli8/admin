const state = {
 userName:"chenli"
};
const actions = {
};
const getters={
  age: state => state.userName
};

const mutations = {

};
export default {
  // namespaced: true,
  state,
  getters,
  actions,
  mutations,
}
